SELECT
  CASE
    WHEN S.MARKS < 70
      THEN NULL
    ELSE S.NAME
  END,
  G.GRADE,
  S.MARKS
FROM 
  STUDENTS S, 
  GRADES G
WHERE 
  S.MARKS >= G.MIN_MARK
  AND S.MARKS <= G.MAX_MARK
ORDER BY
  G.GRADE DESC,
  S.NAME;